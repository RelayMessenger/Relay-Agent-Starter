# Agent Token from Relay Console. Relay shows it once when you create an agent.
RELAY_AGENT_TOKEN=rly_live_...
# signing_secret from POST /v1/webhook-subscriptions. Relay never returns it again.
RELAY_WEBHOOK_SECRET=whsec_...
